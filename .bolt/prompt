import {
  initRouter
} from './router.js';
import * as db from './database.js';

window.db = db; // Expondo para testes e debug no console

document.addEventListener('DOMContentLoaded', () => {
  initRouter(db);
});